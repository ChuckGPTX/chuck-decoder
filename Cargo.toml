
[package]
name = "chuck_decoder"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
clap = { version = "4.4", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"  # <--- NEW: Added JSON support
